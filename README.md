# WatchGuard Config Downloader

## Requirements
- NodeJS

## Usage

- Install npm packages: `npm install`
- Create a new user with "status" access.
- Run: `node index.js %hostname% %username_status% %password%`
